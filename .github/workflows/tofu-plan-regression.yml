# Catches layers that propose unexpected changes on a steady-state plan.
# Runs after every tofu-plan on main/PR; fails if any layer has a non-zero
# resource diff.
#
# Reads plan.json (rendered upstream by tofu-layer.yml so providers and
# var.account_key are already in scope). Pure jq + bash — no tofu init.
name: tofu-plan-regression
on:
  workflow_run:
    workflows: [tofu-plan]
    types: [completed]
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' || github.event_name == 'workflow_dispatch' }}
    strategy:
      fail-fast: false
      matrix:
        layer: [01-contabo-infra, 02-oracle-infra, 02-onprem-infra, 03-talos]
    steps:
      - uses: actions/checkout@v5
      # The three per-account-state layers (01-contabo-infra,
      # 02-oracle-infra, 02-onprem-infra) upload one artifact per
      # account (tfplan-<layer>-<acct>); 03-talos uploads a single
      # tfplan-<layer>. Glob with `*` so the regression check covers
      # every per-account artifact while staying a single download for
      # 03-talos. With v6 + multi-match the artifacts land in
      # `artifacts/<artifact-name>/` — `find` walks them all uniformly.
      - uses: actions/download-artifact@v6
        with:
          pattern: tfplan-${{ matrix.layer }}*
          run-id: ${{ github.event.workflow_run.id }}
          github-token: ${{ secrets.GITHUB_TOKEN }}
          path: artifacts

      - name: Assert zero resource diffs
        run: |
          set -euo pipefail
          shopt -s nullglob
          mapfile -t plans < <(find artifacts -name plan.json -type f 2>/dev/null)
          if (( ${#plans[@]} == 0 )); then
            echo "::error::no plan.json artifact found for ${{ matrix.layer }}"
            exit 1
          fi
          rc=0
          for p in "${plans[@]}" ; do
            n=$(jq '[.resource_changes[] | select(.change.actions != ["no-op"] and .change.actions != ["read"])] | length' "$p")
            echo "::notice::artifact=$p non-noop-diffs=$n"
            if [ "$n" -ne 0 ]; then
              jq '.resource_changes[] | select(.change.actions != ["no-op"] and .change.actions != ["read"]) | {address,actions:.change.actions}' "$p"
              echo "::error::$p proposes $n changes on a steady-state plan"
              rc=1
            fi
          done
          exit "$rc"
