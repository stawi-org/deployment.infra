# Editors / OS
.DS_Store
.vscode/
.idea/

# OpenTofu / Terraform
**/.terraform/
**/.terraform.lock.hcl
**/terraform.tfstate
**/terraform.tfstate.backup
**/tfplan
**/*.tfplan

# Local operator overrides (never commit)
tofu/layers/**/terraform.tfvars.local
tofu/layers/**/override.auto.tfvars

# Claude session state
.claude/
WORK_STATE.md
.worktrees/

credentials/
providers/

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Virtual environments (highly recommended)
venv/
env/
.env/
ENV/

# Distribution / packaging
dist/
build/
*.egg-info/

# Unit test / coverage reports
.coverage
htmlcov/
.pytest_cache/

# Sensitive environment variables
.env
.venv


# Talos layer apply-stage scratch (rendered machine configs / talosconfig)
# Contains cluster-CA-signed material; never commit.
tofu/layers/03-talos/.apply-stage/
